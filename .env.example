# LinkedIn credentials
LINKEDIN_EMAIL=
LINKEDIN_PASSWORD=

# LLM — Gemini uniquement, pas d'Ollama
# Obtenez votre clé sur https://aistudio.google.com/apikey
GEMINI_API_KEY=
LLM_PROVIDER=gemini
LLM_MODEL=gemini-2.0-flash
# Fallback model used when primary hits daily quota (Free-Tier quotas are distinct per model)
GEMINI_FALLBACK_MODEL=gemini-2.0-flash-lite
# Delay in seconds between LLM scoring calls (reduces per-minute rate-limit pressure)
GEMINI_INTER_CALL_DELAY_S=2

# Storage
DB_PATH=./data/linkedin.db
SESSION_PATH=./data/session.json

# Logging
LOG_LEVEL=INFO
LOG_FILE=./logs/agent.log

# Behaviour
DRY_RUN=false
MAX_INVITATIONS_PER_DAY=15
MAX_ACTIONS_PER_DAY=500
# Activity window timezone — uses Python zoneinfo (stdlib, no extra deps)
# Must be a valid IANA timezone name (e.g. Europe/Paris, America/New_York)
ACTIVITY_TIMEZONE=Europe/Paris

# ── RTK log summarization (optional) ─────────────────────────────────────────
# RTK is a Rust binary that summarises noisy log streams via information-gain
# filtering.  The agent uses it to generate a /logs/digest_<run_id>.txt after
# each run.  Set RTK_ENABLED=false to disable entirely without removing config.
#
# Install: cargo install rtk   (or mount the host binary via Docker volume)
# RTK_ENABLED=true
# RTK_BIN=/home/gloaguen/.cargo/bin/rtk   # path to the rtk binary
# RTK_TIMEOUT=30                           # max seconds for rtk subprocess
